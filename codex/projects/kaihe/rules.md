# Kaihe 项目规则

本文件适用于 `/home/ghy/work/kaihe` 和 `/home/hetao/workspace/kaihe`。

## 权威入口与路由

- 仓库根 `AGENTS.md` 是项目规则入口；按其中路由读取目标文档最近层级的 README、`.kilo/rules/` 和匹配的 `.kilo/skills/`，本文件不复制内容规范和执行流程。
- 需求与原型入口先从 `docs/需求文档/README.md`、`docs/产品原型/README.md` 及目标目录的 README 定位；当前文件、Frontmatter、版本历史和真实导航关系共同决定权威版本。
- 用户指定具体文件或路径时先检查该来源；未指定时才按 README 路由，不默认读取 `archive/` 或个人历史目录。

## 个人补充与环境边界

- 需求文档用于确认明确业务规则，原型用于核对页面、字段、展示对象、触发来源和交互；跨文档结论必须标明是直接依据还是推断。
- 原型分析需追踪入口页、导航、链接或 iframe 关系，不能只看孤立页面。
- 环境路径和外部资料只从当前环境规则或用户明确提供的位置获取，不把某台机器的绝对路径写成项目通用事实。
